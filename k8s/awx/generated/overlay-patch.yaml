# This file is regenerated by `make overlay-generate` from .env and
# the corporate CA bundle (if present). The default committed version
# is a no-op patch.
apiVersion: awx.ansible.com/v1beta1
kind: AWX
metadata:
  name: awx
spec: {}
